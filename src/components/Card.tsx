'use client';

import React from 'react';
import { Box } from '@mui/material';

const Card = () => {
  return (
    <Box
      sx={{
        width: '345px', 
        height: '560px', 
        backgroundColor: '#D1D3CA',
       
      }}
    />
  );
};

export default Card;
